name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    name: Test Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install xvfb (Linux only — dla testów GUI)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y xvfb python3-tk

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Lint with ruff
      run: |
        ruff check .
        ruff format --check .

    - name: Type check with mypy (wszystkie platformy)
      # Sprawdzamy każdą platformę jawnie — kod warunkowany sys.platform
      # może mieć błędy widoczne tylko pod konkretnym --platform (np. unreachable).
      run: |
        mypy --platform linux src/
        mypy --platform darwin src/
        mypy --platform win32 src/

    - name: Test with pytest (Linux)
      if: runner.os == 'Linux'
      run: |
        xvfb-run -a pytest --cov=src/epubforge --cov-report=xml -m "not integration"

    - name: Test with pytest (Windows/Mac)
      if: runner.os != 'Linux'
      run: pytest --cov=src/epubforge --cov-report=xml -m "not gui and not integration"

    - name: Upload coverage (Linux + Python 3.12 only)
      if: runner.os == 'Linux' && matrix.python-version == '3.12'
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        fail_ci_if_error: false
