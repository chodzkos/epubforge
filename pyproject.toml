[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "epubforge"
dynamic = ["version"]
description = "Modern modular toolkit for EPUB files — validate, fix, convert, hyphenate"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "chodzkos" }
]
keywords = ["epub", "ebook", "kindle", "kfx", "hyphenation", "metadata"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Text Processing :: Markup",
    "Topic :: Office/Business",
    "Typing :: Typed",
]

dependencies = [
    "lxml>=5.0",
    "pyphen>=0.15",
    "tinycss2>=1.3",
    # Raport CLI `doctor`/`info` — tabele i kolory (warstwa prezentacji, nie core).
    "rich>=13.7",
    # Lokalizacja configu wg konwencji systemu (GUI_STANDARD §8) — core, bez Qt.
    "platformdirs>=4.0",
    # Wspólny rdzeń GUI (paleta + config) — warstwa 0 kitu, czysty Python bez Qt;
    # core/CLI używają chodzkos_gui_kit.config i .palette. Tor Qt dokłada extra [qt].
    # Pin po pełnym SHA commitu taga (supply-chain): tag mutowalny, SHA nie. v0.5.0.
    "chodzkos-gui-kit @ git+https://github.com/chodzkos/gui-kit@09938082b2b5d2250fb1adfb2fd238a8199366ec",  # v0.5.0
]

[project.optional-dependencies]
gui = [
    # Motyw/dialogi/titlebar pochodzą z chodzkos-gui-kit (ekstrakcja P1) — extra [qt]
    # dokłada PySide6. Dolny pułap 6.8 wymagany przez styleHints().colorScheme()
    # (Qt 6.5+); górny luźny, by projekt instalował się też na nowszym Pythonie.
    "PySide6>=6.8,<7",
    # Pin po pełnym SHA commitu taga (supply-chain) — musi zgadzać się z torem core. v0.5.0.
    "chodzkos-gui-kit[qt] @ git+https://github.com/chodzkos/gui-kit@09938082b2b5d2250fb1adfb2fd238a8199366ec",  # v0.5.0
]
stats = [
    # Wykrywanie języka treści w statystykach; opcjonalne — bez niego język bierzemy
    # z metadanych EPUB-a (fallback).
    "langdetect>=1.0.9",
]
dev = [
    "babel>=2.14",
    "pytest>=8.0",
    "pytest-cov>=4.1",
    "pytest-mock>=3.12",
    "pytest-qt>=4.4",
    "ruff>=0.4",
    "mypy>=1.10",
    "pre-commit>=3.7",
    "pdoc>=14.0",
    "lxml-stubs",
]
build = [
    "babel>=2.14",
    "pyinstaller>=6.6",
    "Pillow>=10.0",
]

[project.scripts]
epubforge = "epubforge.cli.main:main"

[project.gui-scripts]
epubforge-gui = "epubforge.gui.app:main"

[project.urls]
Homepage = "https://github.com/chodzkos/epubforge"
Documentation = "https://github.com/chodzkos/epubforge/blob/main/README.md"
Repository = "https://github.com/chodzkos/epubforge"
Issues = "https://github.com/chodzkos/epubforge/issues"
Changelog = "https://github.com/chodzkos/epubforge/blob/main/CHANGELOG.md"

[tool.hatch.version]
path = "src/epubforge/__init__.py"

# Zależność chodzkos-gui-kit jest referencją bezpośrednią (git+https @ SHA) — do
# czasu publikacji kitu na PyPI hatchling wymaga jawnej zgody na takie odwołania.
[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/epubforge"]

[tool.hatch.build.targets.wheel.force-include]
"src/epubforge/locale" = "epubforge/locale"
"src/epubforge/fixers/presets" = "epubforge/fixers/presets"
"src/epubforge/stats_stopwords" = "epubforge/stats_stopwords"

# ── uv (reprodukowalne buildy, REL-3) ───────────────────────────────────────────
# `uv.lock` (wersjonowany) jest źródłem prawdy o wersjach: przypina wszystkie
# zależności — w tym git-referencję chodzkos-gui-kit (po commicie) i transitive
# z hashami. CI instaluje z locka przez `uv sync --frozen`, więc artefakty (.exe)
# i testy powstają z tych samych wersji. `pip install -e ".[dev,gui]"` nadal działa
# dla lokalnego dev-workflow (lock jest addytywny, pip go ignoruje).
[tool.uv]
required-version = ">=0.8"

# ── Ruff (linter + formatter) ──────────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # bugbear
    "C4",   # comprehensions
    "UP",   # pyupgrade
    "N",    # pep8-naming
    "SIM",  # simplify
    "RUF",  # ruff-specific
]
ignore = [
    "E501",  # line too long (formatter handles it)
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]  # asserts dozwolone w testach

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# ── Mypy (type checker) ────────────────────────────────────────────────────────
[tool.mypy]
python_version = "3.10"
strict = true
warn_unreachable = true
show_error_codes = true
files = ["src/epubforge"]

[[tool.mypy.overrides]]
module = [
    "pyphen",
    "tinycss2",
    "langdetect",
]
ignore_missing_imports = true

# ── Pytest ─────────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
]
markers = [
    "gui: testy GUI (wymagają PySide6; w CI offscreen)",
    "integration: testy integracyjne (wymagają zewnętrznych narzędzi)",
    "slow: wolne testy",
]

# ── Coverage ───────────────────────────────────────────────────────────────────
[tool.coverage.run]
source = ["src/epubforge"]
branch = true
omit = ["*/gui/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]
